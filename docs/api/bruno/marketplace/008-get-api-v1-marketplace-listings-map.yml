info:
  name: List map marketplace
  type: http
  seq: 8
http:
  method: GET
  url: '{{base_url}}/api/v1/marketplace/listings/map/'
  auth:
    type: none
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: '# GET /api/v1/marketplace/listings/map/


  Authentication: **none**.


  ## Request field notes


  No path or query parameters.


  ## Saved examples


  Examples are static source-backed contract fixtures. Capture runtime responses with
  the local environment when fixtures and providers are available.'
examples:
- name: success
  request:
    url: '{{base_url}}/api/v1/marketplace/listings/map/'
    method: GET
    headers:
    - name: accept
      value: application/json
  response:
    status: 200
    statusText: OK
    headers:
    - name: content-type
      value: application/json
    body:
      type: json
      data: "{\n  \"success\": true,\n  \"message\": \"OK\",\n  \"data\": {}\n}"
